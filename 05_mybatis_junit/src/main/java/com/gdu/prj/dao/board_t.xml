<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdu.prj.dao.board_t"> <!-- 이 파일의 경로 작성함. -->

  <insert id="insertBoard"
          parameterType="com.gdu.prj.dto.BoardDto">
    INSERT INTO BOARD_T (
        BOARD_NO
      , TITLE
      , CONTENTS
      , MODIFIED_AT
      , CREATED_AT
    ) VALUES (
        BOARD_SEQ.NEXTVAL
      , #{title}
      , #{contents}
      , CURRENT_DATE
      , CURRENT_DATE
    )
    
  </insert>
  
  <update id=""></update>
  
  <delete id=""></delete>
  
  <select id="selectBoardList"
          parameterType="Map"
          resultType="">
    SELECT RN, BOARD_NO, TITLE, CONTENTS, MODIFIED_AT, CREATED_AT
      FROM (SELECT ROW_NUM() OVER(ORDER BY BOARD_NO ${sort}) AS RN, BOARD_NO, TITLE, CONTENTS, MODIFIED_AT, CREATED_AT
              FROM BOARD_T)
     WHERE RN BETWEEN #{begin} AND #{end}
  </select>
  
</mapper>